apiVersion: apps/v1
kind: Deployment
metadata:
  name: commands-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: servicescommands
  template:
    metadata:
      labels:
        app: servicescommands
    spec:
      containers:
      - name: servicescommands
        image: wisdomnwachukwu/servicescommands:latest
        # resources:
        #   limits:
        #     memory: "128Mi"
        #     cpu: "500m"
        # ports:
        # - containerPort: <Port>

---
apiVersion: v1
kind: Service
metadata:
  name: commands-cluster-ip-srv
spec:
  type: ClusterIP
  selector:
    app: servicescommands
  ports:
  - name: servicescommands
    protocol: TCP
    port: 80
    targetPort: 80

